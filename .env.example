# Backend API base URL used by the frontend (src/services/api.js).
# Local dev default is http://localhost:5000 — set this to your deployed
# API domain in production, e.g. https://api.yourdomain.com
VITE_API_URL=http://localhost:5000
